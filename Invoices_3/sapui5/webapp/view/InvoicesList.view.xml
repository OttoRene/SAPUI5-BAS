<mvc:View   controllerName="logaligroup.SAPUI5.controller.InvoicesList"
            xmlns="sap.m"
            xmlns:mvc="sap.ui.core.mvc">
    <List   id="invoiceId"
            headerText="{i18n>InvoiceListTitle}" 
            class="sapUiResponsiveMargin" 
            width="auto" 
            items="{ path : 'northwind>/Invoices',
                     sorter : { path : 'ShipperName',
                     group  : true }
                    }">
        <headerToolbar>
            <Toolbar>
                <Title text= "{i18n>InvoiceListTitle}"></Title>
                <ToolbarSpacer/>
                <SearchField width="50%" search=".onFilterInvoices"/>
            </Toolbar>
        </headerToolbar>
        <items>
        <!-- number type: redondea el total 
             numberState = Error, Information, none, success, Warning 
             numberState="Warning" -->
            <ObjectListItem title="{northwind>Quantity} x {northwind>ProductName}"
                            number="{ parts : [ {path : 'northwind>ExtendedPrice'},
                                                {path : 'currency>/eur'}],
                                      type: 'sap.ui.model.type.Currency',
                                      formatOptions: {
                                          showMeasure: false                              
                                      } }"
                            numberUnit="{currency>/eur}"
                            numberState="{= ${northwind>ExtendedPrice} > 50 ? 'Success' : 'Error'}">
<!--                <firstStatus>
                    <ObjectStatus text="{ path : 'northwind>Status',
                                          formatter: '.formatter.invoiceStatus' }" ></ObjectStatus>
                </firstStatus> -->
            </ObjectListItem>
        </items>
    </List>

</mvc:View>
